import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uic-mobile-app-concept',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
